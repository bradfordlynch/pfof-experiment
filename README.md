# Randomized Experiment  on Order Execution Quality
Study of order execution quality at PFOF and non-PFOF brokers.

## Environment setup
The experiment code expects the following environment variables be set.
```
export PG_API_KEY="your Polygon.io credentials"
export AWS_DEFAULT_REGION="your-aws-region"
```

## Running an experiment